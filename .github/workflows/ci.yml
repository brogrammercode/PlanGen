# name: CI - Plangen

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:15
#         ports: ["5432:5432"]
#         env:
#           POSTGRES_USER: plangen
#           POSTGRES_PASSWORD: secret
#           POSTGRES_DB: plangen_db
#         options: >-
#           --health-cmd="pg_isready -U plangen"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # ---------- Node.js Projects ----------
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: "npm"

#       - name: Install dependencies for API Gateway
#         working-directory: backend/api-gateway
#         run: npm ci

#       - name: Build API Gateway
#         working-directory: backend/api-gateway
#         run: npm run build

#       - name: Run tests for API Gateway
#         working-directory: backend/api-gateway
#         run: npm test

#       - name: Install & Test Auth Service
#         working-directory: backend/services/auth-service
#         run: |
#           npm ci
#           npm run build
#           npm test

#       # ---------- Python / FastAPI Projects ----------
#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: 3.11

#       - name: Install FastAPI dependencies
#         run: |
#           pip install -r backend/services/template-service/requirements.txt
#           pip install -r backend/services/plan-service/requirements.txt
#           pip install pytest

#       - name: Run tests for Template Service
#         working-directory: backend/services/template-service
#         run: pytest

#       - name: Run tests for Plan Service
#         working-directory: backend/services/plan-service
#         run: pytest

#   docker-build:
#     needs: build-and-test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to DockerHub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Build & Push Images
#         run: |
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/plangen-api-gateway:latest backend/api-gateway
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/plangen-api-gateway:latest
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/plangen-auth-service:latest backend/services/auth-service
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/plangen-auth-service:latest
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/plangen-template-service:latest backend/services/template-service
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/plangen-template-service:latest
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/plangen-plan-service:latest backend/services/plan-service
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/plangen-plan-service:latest
